/**
 * GetJob - API Configuration
 * Update API_BASE_URL for production deployment
 */
const CONFIG = {
  API_BASE_URL: 'http://localhost:8080/api',
  USE_API: true  // Set to false to use localStorage only (works without backend)
};
